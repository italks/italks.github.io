node_modules
docs/.vitepress/cache
docs/.vitepress/dist
config.json

# WorkBuddy 多设备 Memory 配置
# 方案：读取所有设备记录，只写入当前设备（推荐）
# 同步所有设备的 memory，但只修改当前设备的文件

# 保留所有 memory 文件（包括设备专属）
!.workbuddy/memory/
!.workbuddy/memory/devices/
!.workbuddy/memory/devices/**